<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.sys.dao.SysUserDao">
	<sql id="queryWhereId">
		from sys_users
		<where>
			<if test="username!=null and username!=''">
				username like concat("%",#{username},"%")
			</if>
		</where>
	</sql>
	
	<select id="getRowCount" resultType="int">
		select count(*)
		<include refid="queryWhereId"/>
	</select>
	
	<select id="findPageObjects" resultMap="sysUserDept">
		select * 
		<include refid="queryWhereId"/>
		order by createdTime desc limit #{startIndex}, #{pageSize}
	</select>
	
	<select id="findUserByUserName" resultType="com.cy.sys.pojo.SysUser">
		select * from sys_users where username=#{username}
	</select>
	
	<update id="updateObject" parameterType="com.cy.sys.pojo.SysUser">
		update sys_users
		set username=#{username},
			mobile=#{mobile},
			email=#{email}
			deptId=#{deptId},
			modifiedTime=now(),
			modifiedUser=#{modifiedUser}
		where id=#{id}
	</update>
	
	<select id="findObjectById" resultMap="sysUserDept">
		select * from sys_users where id=#{id}
	</select>
	
	<insert id="insertObject" parameterType="com.cy.sys.pojo.SysUser"
		useGeneratedKeys="true"
		keyProperty="id">
		
		insert into sys_users
		(username,username,deptId,email,mobile,salt,valid,
		createdTime, modifiedTime,createdUser,modifiedUser)
		values
		(#{username},#{username},#{deptId},#{email},#{mobile},
		#{salt},#{valid},now(),now(),#{createdUser},#{modifiedUser})
	
	</insert>
	
	<update id="validById">
		update sys_users
		set valid=#{valid},
			modifiedUser=#{modifiedUser},
			modifiedTime=now()
		where id=#{id}
	</update>
	
	
	
	
	<resultMap type="com.cy.sys.vo.SysUserDeptVo" id="sysUserDept">
		<id property="id" column="id"></id>
		<association property="sysDept" column="deptId"
		select="com.cy.sys.dao.SysDeptDao.findById">
		</association>
	</resultMap>
</mapper>